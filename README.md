# FBI Wanted

A Next.js app that shows all FBI wanted persons in a grid layout. Fetches data from the FBI API and shows all wanted persons with images.

Uses the [FBI API](https://api.fbi.gov/docs#/).

Built with the T3 Stack (Next.js, tRPC, Tailwind CSS).
